import styled from "@emotion/styled";
import { Container } from "@mui/system";

export const PokemonTypeContainer = styled(
  Container
)({
  display: "flex",
  flexDirection: "row",
  columnGap: "0.5rem",
});
